import React from 'react';
import styled from 'styled-components';

const GameStatsInfoBox = styled.div`
  width: 90px;
  height: 10px;
`;

function GameStatsInfo(props) {
  return (
      <GameStatsInfoBox>

      </GameStatsInfoBox>
  );
}

export default GameStatsInfo;
